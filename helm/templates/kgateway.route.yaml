apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: duuni-route
  namespace: thanu-ns1
 
spec:
  parentRefs:
    - name: newkgateway1
  rules: #path based routing
    - matches:
      - path:
          type: PathPrefix
          value: /
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
      backendRefs:
        - name: du-svc
          port: 3001
 